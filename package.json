{
  "name": "restaurant-ai-menu",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "cd apps/web && npm ci && npm run build",
    "test": "npm --prefix apps/web test",
    "typecheck": "npm --prefix apps/web run typecheck"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
